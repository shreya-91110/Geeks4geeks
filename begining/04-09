class Solution{
	public:
		int nthStair(int n){
		    int ans = 1;
            
            while(n > 1) {
                ans += 1;
                n -= 2;
            }
            
            return ans;
		}
};
